rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /leads/{leadId} {
      // TEMPORARY: Allow writes so API route can work
      // TODO: Set up Firebase Admin SDK for proper server-side writes
      allow read: if false;
      allow write: if true;  // Temporary - allows API route to write
    }
  }
}

